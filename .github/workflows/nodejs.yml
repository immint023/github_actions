name: Greeting from Mona

on:
  - push

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [10.x]
    steps:
      - name: Print a greeting
        run: |
          npm install
          echo "hihi"
